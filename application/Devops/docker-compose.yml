version: "3.8"
services:
  webserver:
    build:
      context: ../frontend
      dockerfile: Dockerfile.frontend
    ports:
    - 80:80
  webapp:
    build:
      context: ../Backend
      dockerfile: Dockerfile.backend
    ports:
    - 5000:5000