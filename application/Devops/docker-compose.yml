version: "3.8"
services:
  webserver:
    build:
      context: ../Frontend
      dockerfile: Dockerfile.frontend
    ports:
    - 8080:80
  webapp:
    build:
      context: ../Backend
      dockerfile: Dockerfile.backend
    ports:
    - 5000:5000